[project]
name = "rag-packages"
version = "0.1.0"
description = "Shared packages for the RAG backend"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
